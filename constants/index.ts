import { abi as publicContractAbi } from "./public_lock_abi";
export {
  UNLOCK_FACTORY_ADDRESSES,
  getUnlockFactoryAddress,
} from "./unlock_factory_addresses";

export const PUBLIC_LOCK_CONTRACT = {
  abi: publicContractAbi,
};
